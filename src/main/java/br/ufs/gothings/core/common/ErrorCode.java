package br.ufs.gothings.core.common;

/**
 * @author Wagner Macedo
 */
public enum ErrorCode {
    INVALID_URI,
    UNAVAILABLE_PLUGIN,
    TARGET_NOT_FOUND,
    PATH_NOT_FOUND,
    OTHER,
    INTERNAL_ERROR,
}
