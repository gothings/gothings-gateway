package br.ufs.gothings.core.common;

/**
 * @author Wagner Macedo
 */
public enum Reason {
    INVALID_URI,
    UNAVAILABLE_PLUGIN,
    TARGET_NOT_FOUND,
    PATH_NOT_FOUND,
    OTHER,
    INTERNAL_ERROR,
}
